"use strict";

import * as debugModule from 'debug';
import {ContainerInterface} from "./Util/Container/ContainerInterface";

let debug = debugModule('services');

export default (function() {

    return {
        setup: function(container: ContainerInterface) {

        }
    }
})();

